<UserControl x:Class="WpfApplication3.EditControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="400"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        <Grid x:Name="grdDisplay" HorizontalAlignment="Left" Height="240" Margin="10,10,0,0" VerticalAlignment="Top" Width="772">
            <ScrollViewer >
                <StackPanel x:Name="spDisplay"/>
            </ScrollViewer>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button x:Name="btnSimpleCondition" Grid.Column="0" Margin="5" Click="btnSimpleCondition_Click"/>
            <Button x:Name="btnComplexCondition" Grid.Column="1" Margin="5" Click="btnComplexCondition_Click"/>
            <Button x:Name="btnCreate" Grid.Column="2" Margin="5" Click="btnCreate_Click"/>
        </Grid>
    </Grid>
</UserControl>
